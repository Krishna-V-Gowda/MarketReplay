# Contributing

Changes to a message layout must include a specification reference, a valid binary fixture, a negative-length test, a Python-oracle update, and a differential case. Changes to transition semantics must include an invariant or failure-mode test. Run `make verify` before opening a pull request.
